
/* Función declarativa es cuando se declara la función */
function example(a, b, c){
    return a+b+c
    }

/* Función de expresión es cuando se omite la declaración y se lleva a una variable o constante con función anónima */    
const example = function(a, b, c){
    return a+b+c
}